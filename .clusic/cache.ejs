const Cache = require('@clusic/cache');
const { expression, expire } = require('@clusic/cache/help');

module.exports = class <%=className%> extends Cache {
  constructor(...args) {
    super(...args);
  }
};